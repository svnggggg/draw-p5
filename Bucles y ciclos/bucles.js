// Copiar el codigo y pegarlo en "https://editor.p5js.org/"

function setup() {
    createCanvas(600, 400);
  }

  function draw() {
    background(20);

  for (let x = 0 ; x < 600 ; x+=50){
  fill (x,34,52)
  ellipse(x,200,25,25)

  for (let x = 0 ; x < 600 ; x+=50){
  fill (255,x,67)
  ellipse(x,100,25,25)

  for (let x = 0 ; x < 600 ; x+=50){
  fill (0,x,45)
  ellipse(x,300,25,25)

  for (let x = 0 ; x < 600 ; x+=50){
  fill (0,x,x)
  ellipse(x,400,25,25)

  }
  }
  }
  }
  }